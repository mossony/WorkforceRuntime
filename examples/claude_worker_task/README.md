# Claude Code Worker Task Example

This directory is a placeholder workspace for Phase 8 Claude Code worker
experiments. The `ClaudeCodeWorker` adapter expects the Claude Code CLI to be
available as `claude` and uses headless print mode.
