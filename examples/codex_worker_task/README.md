# Codex Worker Task Example

This directory is a placeholder workspace for Phase 7 Codex worker experiments.
The `CodexWorker` adapter can run against any isolated git workspace and will
capture Codex JSONL output, stderr, final message, and git diff artifacts.
